# Processed datasets

| File | Rows | Role |
| --- | --- | --- |
| `adult_clean.csv` | 48,813 | Human-readable cleaned table + engineered features |
| `adult_model_ready.csv` | 48,813 | Same rows with label encodings (`*_code`) and z-scored numerics (`*_z`) |

Both files have **0 missing cells** and **0 duplicate rows**. Regenerated by `python src/preprocess.py`.
